/// Represents the different views/screens in the app
/// Inspired by TS AppView = 'search'|'detail'|'steps'|'completion'|'history'
enum AppView {
  search,
  detail,
  steps,
  completion,
  history,
}
